# usually 3000, but can be any integer
PORT=<some integer>

# Frontend Vite app
# Use this in Vercel or any other hosted frontend to point at the deployed API.
VITE_SUPABASE_URL=https://zresjeovgicioeanrosm.supabase.co

# Supabase Postgres connection string.
# Paste the "Connection string" from your Supabase project settings.
# If DATABASE_URL is set, backend/config/db.js will use it.
DATABASE_URL="postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres?sslmode=require"

# Optional fallback if you prefer split vars instead of DATABASE_URL.
PGUSER="postgres"
PGPASSWORD="your-password"
PGHOST="db.your-project-ref.supabase.co"
PGDATABASE="postgres"
